/**
 * 체크박스를 만드는 입력 규칙.
 *
 * `[] ` 하나만 받는다. 하이픈은 하이픈으로 남긴다 — 줄 앞에 - 를 찍는 것은
 * 항목을 나열하는 아주 흔한 손버릇이라, 칠 때마다 체크박스가 되면 성가시다.
 * 할 일을 만드는 길이 하나뿐인 것이 외우기에도 낫다.
 */
export const TASK_INPUT_RULE = /^\[\]\s$/
